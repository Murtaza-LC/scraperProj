{
  "name": "market-compare",
  "private": true,
  "type": "module",
  "dependencies": {
    "playwright": "1.46.0"
  },
  "scripts": {
    // optional if you already run install in netlify.toml
    "postinstall": "PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install chromium"
  }
}
